{
  "name": "eod-app",
  "version": "1.0.0",
  "private": true,
  "description": "Static EOD report app. InstaWork sign-out JPGs are saved via eod-api (eod-api.the-dump-bin.com) so mobile works. flow-automation still runs other local automation.",
  "dependencies": {
    "node-cron": "^4.2.1"
  }
}
